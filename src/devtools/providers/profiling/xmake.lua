target("fei-devtools-profiling")
    set_kind("static")
    add_rules("fei.reflect")
    add_headerfiles("include/**.hpp", "src/*.hpp")
    add_files("src/*.cpp")
    add_includedirs("include", {public = true})
    add_deps("fei-devtools", "fei-profiling")

target("fei-devtools-profiling-tests")
    set_kind("binary")
    set_default(false)
    add_rules("fei.test", "fei.reflect")
    add_files("tests/*.cpp")
    add_includedirs("src")
    add_deps("fei-devtools-profiling")
