target("fei-math")
    set_kind("static")
    add_rules("fei.reflect")
    add_headerfiles("**.hpp")
    add_files("*.cpp")
    add_deps("fei-refl")

target("fei-math-tests")
    set_kind("binary")
    set_default(false)
    add_rules("fei.test")
    add_files("tests/*.cpp")
    add_deps("fei-math")
