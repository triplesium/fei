target("fei-math")
    set_kind("static")
    add_headerfiles("**.hpp")
    add_files("*.cpp")

target("fei-math-tests")
    set_kind("binary")
    set_default(false)
    add_files("tests/*.cpp")
    add_packages("catch2")
    add_deps("fei-math")
    add_tests("default")
