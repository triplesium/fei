target("fei-input")
    set_kind("static")
    add_rules("fei.reflect")
    add_headerfiles("include/**.hpp", "include/**.def")
    add_files("src/*.cpp")
    add_includedirs("include", {public = true})
    add_deps("fei-base", "fei-refl", "fei-ecs", "fei-app", "fei-math")

target("fei-input-tests")
    set_kind("binary")
    set_default(false)
    add_rules("fei.test")
    add_files("tests/*.test.cpp")
    add_deps("fei-input")
