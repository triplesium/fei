interface:
  display_name: "DevTools"
  short_description: "Inspect and control running Entisium samples"
  default_prompt: "Use $devtools to inspect a running Entisium sample and diagnose its rendering state."
